from django.urls import path
# from douryou_seller_apis import views
from hlo.views import *  
from hlo import views 
urlpatterns = [
    # path(r'user-login/', UserLoginView.as_view()), 
    # path(r'user-registration/', UserRegistrationView.as_view()), 
    
    # path(r'user-location/', UsersLocationView.as_view()), 

    # path(r'near-user/', GetNewUser.as_view()), 

]

